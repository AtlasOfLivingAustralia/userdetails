apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "ala-userdetails.labels" . | nindent 4 }}
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  name: mysql-rds
spec:
  type: ExternalName
  externalName: {{ .Values.rds.externalName }}
  ports:
    - port: 3306
      targetPort: 3306