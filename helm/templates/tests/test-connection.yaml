apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ala-userdetails.fullname" . }}-test-connection"
  labels:
    {{- include "ala-userdetails.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ala-userdetails.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
